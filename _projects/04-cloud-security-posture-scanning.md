---
title: "Cloud Security Posture Scanning"
excerpt: "delivers a contextual bird's-eye view across hundreds of cloud environments, revealing full-stack risks and exposure at scale."
collection: projects
tags:
  - Platform
  - Cloud
  - Security
---

## TL;DR:

A user interface that brings together multiple pipelines to deliver full-stack visibility across cloud environments, exposing and correlating security risks at both the platform and operating system layers.

![tldr](/images/cspm/tldr.svg)

## Overview

So far, [Agentless Vulnerability Scanning](/projects/01-agentless-vulnerability-scanning), [Discovery and Compliance Scanning](/projects/02-discovery-compliance-scanning), and [Application Vending Pipeline](/projects/03-application-vending-pipeline) have been explored individually.

This section introduces the final piece that brings them together into a cohesive CSPM application: the frontend and the orchestration glue.

## The project

![cspm-pipeline](/images/cspm/pipeline.png)

### Backend

#### 1. Website hosting

The platform user interface is written in NextJS and is hosted as a static website on S3. The build and deploy process is covered in [Account Vending Pipeline](/projects/03-application-vending-pipeline).

Cloudfront fronts the S3 origin, using a custom domain managed through Route53 and an SSL certificate issued by Amazon Certificate Manager.

#### 2. Authentication

Cognito provides user management for the platform. The [Cognito Identity JavaScript SDK](https://www.npmjs.com/package/amazon-cognito-identity-js) is used for user registration and authentication through the `signUp` and `authenticateUser` flows, removing the need to manage a separate database and user model.

#### 3. Actions

API Gateway acts as the bridge between the frontend and backend. It validates Cognito-issued tokens before authorizing reqests to backend services.

For each module (compliance, discovery, vulnerability), three core requests are available:

- Scan now
  - Invokes a Lambda function that initiates [Agentless Vulnerability Scanning](/projects/01-agentless-vulnerability-scanning) or [Discovery & Compliance Scanning](/projects/02-discovery-compliance-scanning)
- Add
- Remove

The _Add_ and _Remove_ requests invoke an integration Lambda responsible for creating or deleting an account's schedule and S3 namespace.

A _Query_ request is also available and covered below.

#### 4. Data fetching

Two components are used to query the Neo4j database:

[Neo4j GraphQL Library](https://neo4j.com/docs/graphql/current/) enables GraphQL access to Neo4j by generating an executable schema from statically supplied type definitions. These definitions are stored in S3.

[Apollo Server](https://www.apollographql.com/docs/apollo-server/deployment/lambda) runs on a VPC Lambda as the GraphQL API. It receives GQL queries and executes the resolvers generated by the library mentioned above. These resolvers transform GQL into Cypher, Neo4j's native querying language, and execute it against the database.

### Frontend

#### 1. Dashboard

![cspm-overview](/images/cspm/overview.png)

#### 2. Discovery

![cspm-discovery](/images/cspm/discovery.png)

#### 2. Vulnerabilities

![cspm-vulnerabilities](/images/cspm/vulnerabilities.png)

#### 3. Compliance

- **Platform level**
  ![cspm-compliance](/images/cspm/compliance.png)

- **OS level**
  ![cspm-misconfigurations](/images/cspm/misconfigurations.png)

#### 4. Query Builder

![cspm-query](/images/cspm/query.png)
